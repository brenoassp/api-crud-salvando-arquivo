# CRUD salvando em arquivo

Essa é uma api simples feita apenas com libs nativas do Go com persistência em arquivo (person.json).

Rotas

GET /person/
GET /person/{id}
POST /person/
DELETE /person/{id}
